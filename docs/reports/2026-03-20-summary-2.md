# 2026-03-20 总结

## 本次完成
- 在 [app/ui/pages/bolt_page.py](/Users/donghang/PycharmProjects/AI-assistant/.worktrees/bolt-input-linkage-reduction/app/ui/pages/bolt_page.py) 补完了剩余 3 组输入减负联动：
- `assembly.tightening_method` 会为 `tightening.alpha_A` 带入推荐值：扭矩法 `1.6`、转角法 `1.2`、液压拉伸法/热装法 `1.1`。
- `introduction.position` 会为 `stiffness.load_introduction_factor_n` 带入推荐值：螺栓头端 `1.0`、螺母端 `0.6`、中间 `0.4`、分布式 `0.5`。
- `fastener.d` 切换标准规格时，会预填 `bearing.bearing_d_inner / bearing.bearing_d_outer` 建议值，减少支承面几何手输。
- 上述联动都采用“仍处于自动推荐状态才覆盖”的策略，用户一旦手动改值，后续切换选项不会强行冲掉。
- 为了兼容页面初始化自动加载样例、以及 JSON 回灌时常见的 `11` / `11.0` 这种格式差异，自动推荐判断改成了“数值等价”比较。
- 在 [tests/ui/test_bolt_page.py](/Users/donghang/PycharmProjects/AI-assistant/.worktrees/bolt-input-linkage-reduction/tests/ui/test_bolt_page.py) 新增了 6 个 UI 回归测试，覆盖自动推荐、手工覆盖保留、规格预填三类行为。

## 验证结果
- `QT_QPA_PLATFORM=offscreen python3 -m pytest tests/ui/test_bolt_page.py -k "alpha_a_recommendation or load_introduction_factor or bearing_geometry" -v`：`5 passed`
- `QT_QPA_PLATFORM=offscreen python3 -m pytest tests/ui/test_bolt_page.py -v`：`28 passed`
- `QT_QPA_PLATFORM=offscreen python3 -m pytest tests/core/bolt/test_calculator.py tests/core/bolt/test_compliance_model.py tests/ui/test_bolt_page.py -q`：`115 passed`
- `QT_QPA_PLATFORM=offscreen python3 - <<'PY' ... PY`：做了一轮 headless 交互走查，实际输出为：
- `alpha default: 1.6`
- `alpha after angle: 1.2`
- `alpha manual preserved: 1.45`
- `n default: 1.0`
- `n after nut-side: 0.6`
- `n manual preserved: 0.78`
- `bearing default: 11 / 18`
- `bearing after M12: 13 / 22`
- `bearing manual preserved: 12 / 20`
- 未运行：真人桌面 GUI 点击式走查与视觉检查。

## 问题与反思
- 这轮“手工值保留”是通过“当前值是否仍等于上一次自动推荐值”来判断的，逻辑足够轻量，适合当前页面；如果后续类似字段继续增加，可以考虑抽成更明确的 auto/manual 状态。
- 支承面内外径预填目前是“规格建议值”，不是和具体垫圈、螺母、法兰标准强绑定的精确几何；所以字段继续保持可编辑是必要的。
- 当前环境无法进行真正的桌面人工点选，因此补了一轮独立于 pytest 的 headless 交互走查来替代最低限度的交互确认。

## 后续建议
- 如果下一轮继续减输入，优先考虑把支承面几何预填细化为“按连接形式/垫圈标准/螺母端与头端分离”的更准确模型。
- 可以给 `αA`、`n`、支承面几何增加“恢复推荐值”操作，避免用户手改后想回退时要手工再录。
- 条件允许时，补一次真正的桌面 GUI 点选和视觉确认，重点看推荐值变化时页面提示是否足够清楚。

# 蜗轮负载能力校核 Method B（DIN 3996:2019）

## 一图总览

```
  输入扭矩 T2
      │
      ▼
  法向力分解到齿面 Fn = f(T2, d2, γ, α)
      │
      ├─► 齿面赫兹应力 σH ──► 许用 σH_allow ──► 安全系数 SH = σH_allow / σH
      │
      └─► 齿根弯曲应力 σF ──► 许用 σF_allow ──► 安全系数 SF = σF_allow / σF
                                                         │
                                              SH、SF ≥ 目标值 → 通过
```

工况系数 KA、动载系数 Kv、齿宽方向分布 KHβ、横向分配 KHα 在应力计算前作为乘性修正进入 Fn。

## 解决什么问题

给定几何（DIN 3975 派生的 d2、γ、接触线长）、材料（钢-青铜、钢-塑料等副的许用应力）和工况（T2、转速 n、工作时长、冲击等级），判断齿面（点蚀）与齿根（折齿）在长期运转下是否安全。输出两个独立的安全系数 SH、SF，由工程师对照目标值拍板是否放行。

## 核心流程（5 步）

1. 查表取工况系数 KA（平稳驱动/轻冲击/重冲击查 DIN 3996 附录）。
2. 计算动载系数 Kv，主要依赖蜗轮节线速度与齿面精度等级。
3. 计算齿宽载荷分布系数 KHβ（齿宽方向不均匀）与横向载荷分配系数 KHα（齿廓方向分配）。
4. 合并上述修正，计算齿面赫兹应力 σH 与齿根弯曲应力 σF（均为单点解析值）。
5. 与材料许用 σH_allow、σF_allow 对比，得 `SH = σH_allow / σH`、`SF = σF_allow / σF`，对照目标安全系数判通过。

## 本模块实现的范围 / 不实现的范围

> **重要口径声明**：本模块不是完整的 DIN 3996:2019 Method B 实现，而是"Method B 风格的工程简化估算器"。以下描述均指本模块内部实现，**未与原始标准逐条比对**。正式设计评审请以 DIN 3996 原文为准。

**已实现（简化子集）**：
- **齿面应力**：线接触 Hertz 近似计算 σH（`core/worm/calculator.py:_mean_hertz_stress`），而非 Method B 的 ZH/ZE/Zβ 系数链条
- **齿根应力**：等效悬臂梁近似 σF = M·6/(b·s²)（`_root_stress`），而非 Method B 的 YF/YS/Yβ 链条
- **载荷修正**：KA / Kv / KHα / KHβ 四个系数支持手工输入（无内置查表）
- **许用应力库**：**仅**钢-塑料副（PA66、PA66+GF30）内置默认值；钢-青铜副需用户手工填入
- **附加**：基于 Wöhler 曲线 m=6 的寿命估算 N_L，基于 DIN 3996 K9 参考值的磨损率估算

**未实现**：
- Method A（沿接触线逐段数值积分，需要完整疲劳曲线与油膜厚度实测）
- Method C（需 FEA 输入，当前版本选中后 calculator 抛 InputError）
- 完整 DIN 3996 Method B 的 ZH/ZE/Zβ/YF/YS 系数链（当前用 Hertz 线接触 + 悬臂梁两套更粗的近似代替）
- 累积磨损与寿命谱积分（当前仅对单一稳态工况给安全系数）
- 齿面瞬时接触温度与润滑油膜厚度校核（需配合 ISO/TR 14521 或 AGMA 补充）
- 青铜蜗轮副许用应力内置库（用户需自行查表填入）

> Cannot verify against original DIN standard —— 本文涉及 DIN 3996 条款编号、符号体系与 Method A/B/C 边界描述均基于公开文献与代码注释整理，未查证 2019 版正文。

## 常见误用

- **KHα 与 KHβ 混淆**。KHα（alpha，齿廓方向横向分配）与 KHβ（beta，齿宽方向分布）是两个独立的系数，作用方向互相垂直。新手常把齿宽不均填到 KHα，或把齿精度影响填到 KHβ，导致应力整体偏高或偏低。
- **许用应力不分单双向工况**。双向（正反转）运转时，齿面/齿根都要按对称循环折减；塑料蜗轮尤其显著，σF_allow 相比单向可能要再降 30-50%。如果直接套用单向许用值，会得到虚高的安全系数。
- **把"SH = 1"当通过**。工程实践要求 SH ≥ 1.0-1.2、SF ≥ 1.3-1.5（具体目标视可靠性要求、维护难度而定）。等于 1 只代表应力刚好等于许用，没有任何裕度，不能视为合格。

## 参考文献

- DIN 3996:2019 《蜗轮副承载能力计算》（Method A / B / C 方法族与 KA / Kv / KHα / KHβ 查表依据的标准出处）
- ISO/TR 14521 作为润滑与温度校核的补充参考

> Cannot verify against original DIN standard —— 本文未引用 DIN 3996:2019 的精确节号 / 附录编号，因为条款号尚未对照原始标准正文核实；所列章节归属与系数分类基于公开教科书和模块代码注释整理，仅作索引入口。精确引用请以原文为准。

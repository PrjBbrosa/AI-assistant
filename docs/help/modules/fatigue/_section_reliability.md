# 损伤与可靠性

Miner 损伤为 `D=sum(ni/Ni)`。设计损伤使用指定存活率的 P-S-N 曲线；目标损伤不超过 `1` 只是线性累计假设下的预校核。

Monte Carlo 中，同一模拟零件在全部谱级共享一个 S-N 纵向散差。可选整体载荷倍率服从均值为 `1`、由 COV 确定的对数正态分布。固定随机种子用于结果复现。

`Pf` 是目标谱块数之前失效的概率，`R=1-Pf`。存活率寿命明确写成 `Ps=90%` 等，不使用方向不清的 `N90`。

**出处**：工程统计预校核模型；正式放行需按项目标准确认

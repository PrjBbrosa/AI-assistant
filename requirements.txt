# Lower bounds only. Pin exact versions in constraints.txt.
# Install: python -m pip install -r requirements.txt -c constraints.txt
PySide6>=6.8.0
pyinstaller>=6.10.0
reportlab>=4.0
matplotlib>=3.8.0
openpyxl>=3.1
numpy>=2.0
scipy>=1.14
